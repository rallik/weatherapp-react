import React from 'react'

const Error = () => {
    console.count('error');
    return (
        <section>
            Please select a valid location
        </section>
    )
}

export default Error
